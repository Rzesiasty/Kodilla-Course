CREATE TABLE ISSUESLIST
(
ID SERIAL PRIMARY KEY,
NAME VARCHAR (100)
);

CREATE TABLE ISSUES
(
ID SERIAL PRIMARY KEY,
ISSUESLIST_ID BIGINT UNSIGNED NOT NULL,
SUMMARY VARCHAR (30),
DESCRIPTION VARCHAR (1000),
USER_ID_ASSIGNEDTO BIGINT UNSIGNED NOT NULL,
FOREIGN KEY (ISSUESLIST_ID) REFERENCES ISSUESLIST(ID),
FOREIGN KEY (USER_ID_ASSIGNEDTO) REFERENCES USERS(ID)
);

INSERT INTO ISSUESLIST(NAME)

VALUES("ToDo");

INSERT INTO ISSUESLIST(NAME)

VALUES("In progres");

INSERT INTO ISSUESLIST(NAME)

VALUES("Done");

INSERT INTO ISSUES (ISSUESLIST_ID, SUMMARY, DESCRIPTION, USER_ID_ASSIGNEDTO)

VALUES (1, "Work", "Go to work to earn money", 1);


INSERT INTO ISSUES (ISSUESLIST_ID, SUMMARY, DESCRIPTION, USER_ID_ASSIGNEDTO)

VALUES (2, "Shop", "Go to shop for milk", 3);
INSERT INTO ISSUES (ISSUESLIST_ID, SUMMARY, DESCRIPTION, USER_ID_ASSIGNEDTO)
VALUES (2, "Painting", "Paint a beautiful picture", 5);
INSERT INTO ISSUES (ISSUESLIST_ID, SUMMARY, DESCRIPTION, USER_ID_ASSIGNEDTO)
VALUES (1, "Driving", "Drive from Warsaw to Berlin", 1 );
INSERT INTO ISSUES (ISSUESLIST_ID, SUMMARY, DESCRIPTION, USER_ID_ASSIGNEDTO)
VALUES (3, "Fishing", "Go to the lake and catch fish", 4);

COMMIT;



